{
  "name": "poke-backend",
  "version": "1.0.0",
  "description": "Back End section of full stack app created to practice back end SQL operations using data from the PokeAPI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sam Saks-Fithian",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.4"
  }
}
